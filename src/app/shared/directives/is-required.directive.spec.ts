import { AddClassIfRequiredDirective } from './is-required.directive';

describe('IsrequiredDirective', () => {
  it('should create an instance', () => {
    // const directive = new AddClassIfRequiredDirective();
    // expect(directive).toBeTruthy();
  });
});
